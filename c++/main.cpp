#include <iostream>
// angular brackets are for only compiler include paths
// quotes are for every paths, but mainly used for relative paths

int main(){

    std :: cout << "hello world" << std :: endl;
    // think << as a fxn like print in python or printf in c
    std :: cin.get();
}